<div class="row">

  <div class="col s12">
    {{#if session.isAuthenticated}}
      <div class='action-buttons-wrapper'>
        <div class='button-container'>
          {{#link-to 'blogs.new' classNames='waves-effect waves-light btn'}}
            Add a blog
          {{/link-to}}
        </div>
        <div class='button-container'>
          {{#link-to 'recipes.new' classNames='waves-effect waves-light btn'}}
            Add a recipe
          {{/link-to}}
        </div>
        <div class='button-container'>
          {{#link-to 'reviews.new' classNames='waves-effect waves-light btn'}}
            Add a review
          {{/link-to}}     
        </div>   
      </div>
    {{else}}  
      <div id='signin-alert' class='card blue-grey darken-1'>
        <div class='card-content white-text'>
          <span class='card-title'>Hi there !</span>
          <p>It is nice to have you here dear ORA recruiter. Please, in order to add a new item
          proceed with login first.</p>
        </div>
        <div class='card-action'>
          <a href='#' {{action 'closeAlert'}}>Got it</a>
        </div>
      </div>
    {{/if}}
  </div>

  <div class="col s12">
    <ul class='collection'>
      {{#each sortedDescList as |activity|}}
        {{#activity-item activity=activity}}
        {{/activity-item}}  
      {{/each}}
    </ul>
  </div>
  
</div>